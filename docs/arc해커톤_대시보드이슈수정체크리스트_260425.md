# Arc 해커톤 — 대시보드 이슈 수정 체크리스트 (2026-04-25 생성)

**작성 맥락**: 녹화 전 대시보드 최종 점검 중 캡처 4장(FeePersonaExplorer × 2, FeeMatrix+AgentIdentity, PremiumStream+Reliability+SettlementTx)을 교차 검증한 결과, **11건 이슈 + 진행중 1건** 발견. 한밤중이라 내일(2026-04-26) 일괄 수정 예정.

> **우선순위 원칙**: 판사가 5초 안에 거짓말을 발견할 수 있는 것 → 즉시 수정. 숫자가 과장된 것 → 톤 다운. UI 혼란 → 레이블 명확화.

---

## 🟢 이미 수정 완료 (세션 전반부)

- [x] `strategy` 필드 meta_agent에서 덮어쓰기 버그 ([consumers/meta_agent/main.py:204](consumers/meta_agent/main.py:204)) — source strategy 보존 + `meta_classifier` 별도 필드로 분리
- [x] StrategyCards 3-lane 카드 구조 (v1 kimchi / v2 dual / v3 funding) — 각 lane 고유 threshold semantics
- [x] 거래소 구분 UI: `KR · Upbit (KRW)` ↔ `Global · Binance (USDT)` chip, alt venue 명시
- [x] WhyArcCard gas 단위 통일 (USDC 주 · ETH 보조), gwei range 10-50 disclosure

## 🟡 진행중 (세션 후반부 — 부분 수정)

- [ ] **UNKNOWN strategy fallback 수정** ([dashboard/src/lib/strategy_meta.ts:31](dashboard/src/lib/strategy_meta.ts:31))
  - 문제: UNKNOWN이 `id: 'v2'` 로 설정 → stale `gemini_meta` 신호가 v2-sig 배지로 둔갑, v2 count 부풀리고 v1/v3 숨김
  - 세션 중 수정: `id: 'unk'`, `label: 'unlabeled'`, `cssId: 'unk'` 로 분리된 버킷 신설 (TypeScript type 확장 완료)
  - **남은 작업**:
    - [ ] `styles.css` 에 `.strategy-chip.strategy-unk` 중립 회색 스타일 추가
    - [ ] `App.tsx` StrategyMixChip에 unk 세그먼트 추가 (또는 의도적으로 숨김)
    - [ ] bridge 재시작 후 stale `gemini_meta` 9건 제거 (녹화 직전 클린 시작)

---

## 🔴 내일 처리 (미착수 · 긴급도 높은 순)

### 카테고리 A · 데이터 정합성 (판사 5초 falsifiable)

- [ ] **A1. ERC-8004 레지스트리에 v3 funding producer 누락**
  - 파일: [dashboard/src/components/AgentIdentityCard.tsx:29-62](dashboard/src/components/AgentIdentityCard.tsx:29)
  - 현상: AGENTS 배열에 v1(kimchi)·v2(dual)·meta·executor만 있음. v3 funding producer 없음.
  - 모순: Strategy Cards는 3-lane 주장, Premium Stream 필터는 `V3·0`, 레지스트리도 2 producer → 세 군데 모두 v3 부재 증거
  - 수정안:
    1. `producer-funding.json` agent card 작성 (`dashboard/public/.well-known/agent-card/`)
    2. sha256 계산 → contentHash 확보
    3. 실제 on-chain 등록 TX 발생 (scripts 있으면 재사용, 없으면 수동)
    4. `docs/evidence/erc8004_registry.json` 에 row 추가
    5. `AgentIdentityCard.tsx` AGENTS 배열에 5번째 엔트리 추가
  - 대안 (시간 부족시): "registration pending" 상태로 honest label, 녹화 중 "4 registered, 5th in-flight" 대사 준비

- [ ] **A2. Premium Stream 중복 BTC row (v1-kimchi + v2-sig)**
  - 원인: bridge 메모리에 meta_agent 수정 이전 `strategy="gemini_meta"` 신호 9건 잔존
  - 증거: `curl /signals/premium` 결과 — timestamp 1777042931~1777043000 (8건 gemini_meta), 1777048706/1777048979 (2건 정상 보존)
  - 수정: 녹화 직전 bridge 재시작 (raw=500, premium=11, tx_settled=17 전부 초기화)
  - 주의: tx_settled=17 누계도 리셋됨. 녹화 전 warm-up 시간에 다시 누적 필요

- [ ] **A3. v3 funding 신호가 premium stream에 0건**
  - 원인 가설: producer cadence 30s + threshold 0.05%/8h → 실제 funding이 threshold 못 넘는 구간이 많음
  - 또는 demo/run_demo.py 에서 v3 producer 기동 누락 가능성
  - 수정:
    - [ ] `demo/run_demo.py --with-funding` 실제로 signal 발생하는지 확인
    - [ ] 필요시 funding_agent의 entry threshold 임시 완화 (0.0005 → 0.0002) + UI에 "demo threshold softened" 라벨
    - [ ] 녹화 시점 실제 Binance fapi funding rate 미리 체크해서 threshold 조정

### 카테고리 B · 과장 숫자 (Karpathy/Munger inversion)

- [ ] **B1. fee_calculator frequency 공식 지수 스케일링**
  - 파일: [dashboard/src/lib/fee_calculator.ts:54-62](dashboard/src/lib/fee_calculator.ts:54)
  - 현재: `freqMultiplierPerBp = 1.45`, `maxMultiplier = 20`
  - 결과: 9bp edge 개선 → 28× (cap 20×) → 240 trades/coin/day → **$1,173/day on $494 notional × 9 coins = 일 26% 수익률 (APR 9,600%)**
  - 수정안 A (보수): `freqMultiplierPerBp: 1.15`, `maxMultiplier: 4` → 9bp 개선 → ~3.5× → ~42 trades/day, ~$205/day
  - 수정안 B (더 보수): 지수 → linear 또는 sqrt 전환, 혹은 "calibrated on v1.3 day-4 histogram" 출처 표시
  - 판단: 수정안 A로 진행, footer에 `Calibration: 1bp edge → ×1.15 trades (v1.3 live histogram fit)` 명시

- [ ] **B2. meta_agent stub confidence 정확히 1.00 찍힘**
  - 파일: [consumers/meta_agent/main.py:137](consumers/meta_agent/main.py:137)
  - 공식: `min(1.0, abs(avg)*300) * disagree_factor * blended_hr * 2`
  - 강한 premium + hr=0.5(cold start) → 1.0 × 1.0 × 0.5 × 2 = 1.0 정확히
  - ML 배경 판사: *"real model doesn't output 1.00"* 의심
  - 수정: shrinkage 도입 — `confidence = min(0.95, confidence * 0.95)` 또는 Bayesian shrink toward 0.7

- [ ] **B3. "Variable-price settler" vs 균일 $0.0100 TX**
  - 파일: [dashboard/src/components/AgentIdentityCard.tsx:56](dashboard/src/components/AgentIdentityCard.tsx:56)
  - 모순: executor를 "Variable-price settler" 로 주장하지만 settlement TX 10건 모두 정확히 $0.0100
  - 진단 필요:
    - [ ] executor가 실제로 variable pricing을 사용하는지 코드 확인 (consumers/executor_agent)
    - [ ] raw stream에는 $0.2060, $0.5461 등 가변 있음 — signal price와 settlement cost 구분?
    - [ ] "pricing" 과 "settlement cost" 를 같은 패널에 표시하면서 오해 발생한 것인지 확인
  - 수정: 진단 결과에 따라 label 명확화 ("Fixed-fee settler · variable signal pricing") 또는 실제 variable settlement 구현

### 카테고리 C · UI 명확성

- [ ] **C1. FeeMatrix 푸터 `active scope` 와 Fee Persona Explorer 불일치**
  - 파일: [dashboard/src/components/FeeMatrix.tsx:207-211](dashboard/src/components/FeeMatrix.tsx:207), [dashboard/src/App.tsx:246](dashboard/src/App.tsx:246)
  - 현상: FeeMatrix는 v2 카드 내부 ExchangeScopeTabs의 scope를 표시, FeeExplorer는 독립 state. 판사가 Bybit 골라도 FeeMatrix는 Binance 0.17% 표시 가능
  - 수정안 A: FeeMatrix 푸터 scope line 제거 (혼란 원인 제거)
  - 수정안 B: 두 selector 동기화 — FeeExplorer 선택이 v2 scope 에도 반영되도록 lift state
  - 판단: 수정안 A 로 진행 (B는 리스크 큼)

- [ ] **C2. FeeMatrix DOGE n=10 에서 ✓/✗ 판정**
  - 파일: [dashboard/src/components/FeeMatrix.tsx:141-195](dashboard/src/components/FeeMatrix.tsx:141)
  - 문제: n=10 은 통계적으로 얇음, cherry-picked sample 의심 유발
  - 수정: `if (realized[sym].n < 30) marker = '—'` (중립 대시) + row-header에 n 표시 강조
  - 기존 로직: `meanRate > cell.roundTrip` 조건 앞에 `n >= 30` 가드 추가

- [ ] **C3. FeeExplorer "THRESHOLD" 레이블 중복**
  - 파일: [dashboard/src/components/FeeExplorer.tsx](dashboard/src/components/FeeExplorer.tsx)
  - 현상: 같은 패널에 `THRESHOLD (BP): 17` (allocator gate) 와 `BREAK-EVEN THRESHOLD: 0.060%` (r.t. fee floor) — 둘 다 "threshold"
  - 수정: 첫째 → `ALLOCATOR THRESHOLD (BP)`, 둘째 → `BREAK-EVEN (r.t. fee floor)`

- [ ] **C4. FeeExplorer Bybit default VIP 2 — retail 주장과 충돌**
  - 현상: Bybit 설명 = "Retail-accessible USDC promo"인데 기본 선택이 **VIP 2 (30d volume ≥ $5M)**
  - 수정: 각 venue의 `retailDefault` 플래그를 VIP 0/Lv 1 로 통일 + VIP 2 선택 시 "institutional tier — not retail" 경고 chip 표시

- [ ] **C5. Producer Reliability 패널 scope 명확화**
  - 현상: `/producer/reliability` 는 meta_agent 한 명만 tracking, hit_rate 7% (n=28), total_pnl -$1.60
  - 판사 인식: "3-lane mesh 주장인데 생산자가 1명이고 7% 정확도"
  - 근본 원인: outcome feedback이 producer_id="meta_agent" 레벨에서만 집계
  - 수정안 A (간단): 패널 제목 변경 "Meta-layer reliability (promoted signals)" + 부제 "Raw producer tracking upstream — not yet surfaced"
  - 수정안 B (정공법): bridge 에서 meta_agent 재-publish 시 original producer 리스트 보존 → 각 생산자에게 outcome fanout
  - 판단: 수정안 A 로 시간 절약 (B는 bridge 수정 필요)
  - 추가: 7% 는 n=28 로 noise 범위 — sparkline + "n<200 until warm-up" 주의 추가

---

## 📋 실행 순서 (2026-04-26)

### 1차 (1시간 · 데이터 정합성)

1. [ ] bridge 재시작 → stale gemini_meta 제거 (A2)
2. [ ] `strategy_meta.ts` 잔여 작업 완료 — CSS + StrategyMixChip unk segment
3. [ ] v3 funding producer ERC-8004 레지스트리 추가 (A1) — honest "pending" OK
4. [ ] v3 demo signal 실제 발생 확인 (A3) — threshold 조정 or demo 재구성

### 2차 (30분 · 과장 톤다운)

5. [ ] fee_calculator 공식 조정 (B1)
6. [ ] meta_agent stub shrinkage (B2)
7. [ ] "Variable-price settler" 진단 + 라벨 조정 (B3)

### 3차 (45분 · UI 명확화)

8. [ ] FeeMatrix 푸터 정리 (C1)
9. [ ] FeeMatrix n<30 가드 (C2)
10. [ ] FeeExplorer threshold 레이블 (C3)
11. [ ] FeeExplorer VIP default (C4)
12. [ ] Reliability 패널 label 변경 (C5)

### 4차 (15분 · 검증)

13. [ ] 전체 대시보드 로드 → 각 캡처 재현 지점 방문
14. [ ] `curl /signals/premium` 으로 strategy 필드 확인 (v1/v2/v3 균형)
15. [ ] `curl /producer/reliability` 로 label 반영 확인
16. [ ] Fee Persona Explorer 에서 Bybit VIP 0 default 확인 + 계산 결과 보수적

### 최종 (녹화 warm-up)

17. [ ] Bridge 재시작 → 5분 warm-up → tx_settled 자연 누적
18. [ ] Demo 기동 (`python -m demo.run_demo --symbols DOGE,XRP,SOL --duration 600 --with-kimchi --with-funding`)
19. [ ] v1/v2/v3 신호 각각 최소 3건 확인 후 녹화 시작

---

## 🎯 내일 시작 지점 요약 (1분 컨텍스트)

- 오늘까지 strategy_meta.ts 일부 수정. 나머지 11건 미착수.
- 녹화 데드라인: **2026-04-26 당일** (빡빡함)
- bridge 메모리 보존중 (raw=500, premium=11, tx_settled=17) — 오래된 gemini_meta 신호 9건은 수정 후 첫 기동 시 재로드 필요 없음 (disk에서 다시 안 읽음, in-memory 한정)
- 작업 우선순위: **A 카테고리 전부 > B1,B2 > C 나머지**
- 과장 숫자 B1 (fee freq formula) 는 수정시 카드의 $1,173 → $205 로 하락 — 슬라이드·VIDEO 스크립트에도 반영 필요 (`docs/arc해커톤_덱수정방향_260425.md` 에 업데이트 플래그)

---

## ⚠️ 녹화 직전 hygiene

- [ ] demo 프로세스 모두 kill (USDC 보호 — $20 testnet allocation)
- [ ] bridge만 살려서 메모리 state 보존
- [ ] dashboard (vite dev) 살려서 즉시 확인 가능
- [ ] OBS 씬 준비 (`docs/RECORDING_GUIDE_KR.md` 참조)
